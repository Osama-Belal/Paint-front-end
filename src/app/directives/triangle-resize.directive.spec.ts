import { TriangleResizeDirective } from './triangle-resize.directive';

describe('TriangleResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TriangleResizeDirective();
    expect(directive).toBeTruthy();
  });
});
