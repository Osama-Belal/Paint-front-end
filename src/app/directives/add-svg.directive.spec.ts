import { AddSvgDirective } from './add-svg.directive';

describe('AddSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new AddSvgDirective();
    expect(directive).toBeTruthy();
  });
});
