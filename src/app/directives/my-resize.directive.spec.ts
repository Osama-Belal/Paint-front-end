import { MyResizeDirective } from './my-resize.directive';

describe('MyResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyResizeDirective();
    expect(directive).toBeTruthy();
  });
});
