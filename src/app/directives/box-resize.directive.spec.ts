import { BoxResizeDirective } from './box-resize.directive';

describe('BoxResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxResizeDirective();
    expect(directive).toBeTruthy();
  });
});
